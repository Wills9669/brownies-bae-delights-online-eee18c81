
// Centralized export file for all product data
import { browniesList } from './brownies';
import { cakesList } from './cakes';
import { cakeJarsList, cupcakesList, cakePopsList } from './other-products';

// Re-export everything
export { 
  browniesList,
  cakesList,
  cakeJarsList,
  cupcakesList,
  cakePopsList
};
